# 项目上下文信息

- UI update: floating ball now 40px gradient squircle with aligned panel (top set to 0), ball/panel share corner; added opacity setting slider in size section stored at key lda_v5_opacity and applied via --lda-opacity on #lda-root.
